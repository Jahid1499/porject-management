export type ErrorOrSuccessMessageType = {
    message: string
}

export type NodePropsTypes = {
    children: React.ReactNode
}

export type TeamTypes = {
    name: string, color: string, description: string, timestamp: number, id: number, participants: string
}